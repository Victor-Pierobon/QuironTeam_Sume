services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sume
      POSTGRES_PASSWORD: sume
      POSTGRES_DB: sume
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
